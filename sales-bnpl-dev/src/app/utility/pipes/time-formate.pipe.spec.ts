import { TimeFormatePipe } from './time-formate.pipe';

describe('TimeFormatePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFormatePipe();
    expect(pipe).toBeTruthy();
  });
});
